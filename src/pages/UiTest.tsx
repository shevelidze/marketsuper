import React from "react";
import { ui } from "../components";

const UiTest: React.FC = () => {
    return (
        <div>
            <ui.Button></ui.Button>
        </div>
    );
};

export default UiTest;
